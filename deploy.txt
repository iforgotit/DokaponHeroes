sudo git fetch origin master
sudo git reset --hard FETCH_HEAD
sudo npm install node-pre-gyp --unsafe-perm
sudo npm install bcrypt --unsafe-perm

sudo forever start index.js -m5

sudo forever list

sudo forever stop [id]